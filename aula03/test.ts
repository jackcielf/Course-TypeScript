function run() {
    show("Jack");
}

function show(name: string) {
    document.write(name);
}

/* 
    - In TypeScript is better add type the parameter, this will prevent error in the compilation
*/